-r requirements.txt

# 零样本本地视觉模型（Grounding DINO Tiny + SAM 2.1 Hiera Tiny）官方 Transformers 加载。
# 版本与 2026-08-11 在 RTX 3060 Laptop GPU 6GB 验证通过的组合一致，禁止随意升级。
# CUDA torch/torchvision 按官方 download.pytorch.org 单独安装（见 requirements-vision-lock.txt）。
transformers==5.15.0
safetensors==0.8.0
modelscope==1.39.1
opencv-python-headless==5.0.0.93
