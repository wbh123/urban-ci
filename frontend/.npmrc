registry=https://registry.npmmirror.com
strict-ssl=true
# 项目使用 TypeScript 6 / Vite 8 等较新主版本，部分工具（如 openapi-typescript、
# typescript-eslint、vitest）的 peer 声明尚未覆盖这些主版本。此处放宽 peer 校验，
# 实际兼容性以各工具真实执行（type-check / lint / test / build）结果为准。
legacy-peer-deps=true
