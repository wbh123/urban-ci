# 模型下载、训练、CUDA-only ONNX 评估和导出。
# 先根据本机 CUDA 安装匹配的 GPU 版 torch/torchvision，再安装本文件。
-r requirements-real.txt

torch>=2.9,<2.10
torchvision>=0.24,<0.25
onnx>=1.18,<2
huggingface-hub>=0.34,<2
kaggle>=1.7,<2
tqdm>=4.66,<5
